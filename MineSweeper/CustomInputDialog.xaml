<Window x:Class="MineSweeper.CustomInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Grid Size" Height="150" Width="250" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Rows:"/>
        <TextBox Grid.Row="0" x:Name="txtRows" Width="100" Margin="5"/>

        <Label Grid.Row="1" Content="Columns:"/>
        <TextBox Grid.Row="1" x:Name="txtColumns" Width="100" Margin="5"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="OK" Click="OkButton_Click"/>
            <Button Content="Cancel" Click="CancelButton_Click" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
